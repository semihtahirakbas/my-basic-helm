apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tahir-project.fullname" . }}-test-connection"
  labels:
    {{- include "tahir-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tahir-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
