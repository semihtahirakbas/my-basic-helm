apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "basic-nodejs.fullname" . }}-test-connection"
  labels:
    {{- include "basic-nodejs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "basic-nodejs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
